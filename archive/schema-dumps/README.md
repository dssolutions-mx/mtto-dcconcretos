# Schema dumps (archived)

One-off or snapshot SQL files moved from the repository root so they are not mistaken for an active migration pipeline.

- `PURCHASE_ORDERS_DATABASE_MIGRATION.sql`
- `complete_schema.sql`
