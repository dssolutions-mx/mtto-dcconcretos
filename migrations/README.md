# Migrations

Legacy incremental SQL has been moved to **[`archive/legacy-db-migrations/`](../archive/legacy-db-migrations/README.md)**.

Apply database changes via **Supabase MCP** or **Supabase CLI** (`supabase/migrations/` when using the CLI).
