'use client'

/**
 * PR2: dedicated dialogs per conflict type (duplicate asset_id, manufacturer merge, etc.)
 * Stub keeps route imports stable for incremental work.
 */
export function ConflictResolverPlaceholder() {
  return null
}
